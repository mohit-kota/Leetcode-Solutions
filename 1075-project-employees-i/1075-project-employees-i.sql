# Write your MySQL query statement below
select e1.project_id , ROUND(SUM(e2.experience_years)/COUNT(e2.experience_years),2) as average_years from Project e1 join Employee e2 on e2.employee_id = e1.employee_id group by e1.project_id;